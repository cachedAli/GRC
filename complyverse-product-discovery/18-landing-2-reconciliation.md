# Landing-2 reconciliation

`D:\Work\complyverse_nextjs (1)\landing-2-site` is a standalone static CompliVerse marketing prototype, not a connected product frontend. It has no package manifest or backend and keeps marketing data in `data.js`; `app.js` probes/iframes `http://localhost:3000`, which matches the main product’s development port but is not a production integration. Evidence: `landing-2-site/data.js`, `app.js`, `vercel.json`, `D:\complywerse_ai\grc-frontend\package.json`.

Its 19 capability cards and 18-framework carousel are useful design/content inputs only. The main runtime framework source is the 34-file startup catalog documented in `04-frameworks-and-controls.md`.

| Landing claim | Evidence | Reconciliation |
|---|---|---|
| Any standard / global custom framework support | `data.js` framework card | upload/parser code exists; quality and commercial support need validation |
| AI mapping, grouping, deep evidence assessment | `data.js` controls/evidence cards | partial AI services exist; no autonomous-performance claim is safe |
| Human-in-the-loop for every publishing action | `app.js`, `data.js` | approval models exist in modules, but no universal enforcement proof |
| Tenant-scoped SQL-first answers | ComplyChat card | chatbot/tenant/Qdrant/SQL-agent code exists; production grounding/security unverified |
| Live scanner/cloud/SIEM connections | integration cards | provider code/references are partial, not proof every connector is live |
| Auditor deep links or auto-approval | auditor card | portal exists; exact actions need runtime verification |
| Readiness/control KPI values | hardcoded `data.js`/illustrated screens | mock illustration, not proof |
| ISO 27701 carousel entry | framework carousel | not in runtime seed catalog; team confirmation required |

The safest landing narrative is the model-backed flow: `framework → policy/control → evidence → asset/vulnerability/risk → review/report`. Use verified tenant screenshots and exclude hardcoded figures, comparison marks, customer outcomes and claims of “product accuracy.”
